name: test
on: [push]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@master
    - uses: actions/setup-node@v3
      with:
        node-version: 17.x
        cache: yarn
    - run: yarn install
    - run: yarn lint
    - run: yarn test
