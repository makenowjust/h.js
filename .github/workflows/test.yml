name: test
on: [push]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@master
    - uses: actions/setup-node@v6
      with:
        node-version: 24.x
        cache: yarn
    - run: yarn install
    - run: yarn lint
    - run: yarn test
